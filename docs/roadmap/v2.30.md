# Sentori v2.30 — development plan

Status: **in progress 2026-06-10 — wire batch: iOS interruption-level + thread-identifier + channel-importance.** Eleventh version of Push Phase 2. Three small additive wire fields the OS uses to make notification rendering / urgency decisions. Server-only; SDK / dashboard untouched.

Owner: claude + takagi
Date: 2026-06-10

## NEVER rule

- **Zero wire break.** Three additive optional fields.
- **Provider-specific gracefulness.** Each field maps to one provider's concept; other providers ignore the field silently.

## L2 — Work units

| WU | Acceptance |
|---|---|
| **W1** Wire fields | `NativeOptions` gains `interruption_level: Option<String>` (`active` / `passive` / `timeSensitive` / `critical`; iOS 15+ only), `thread_identifier: Option<String>` (iOS notification grouping), `channel_importance: Option<String>` (Android `high` / `default` / `low` / `min`). |
| **W2** APNs mapping | `aps.interruption-level` ← `interruption_level`; `aps.thread-id` ← `thread_identifier`. |
| **W3** FCM mapping | `message.android.notification.notification_priority` ← `channel_importance` (mapped: `high→PRIORITY_HIGH`, `low→PRIORITY_LOW`, etc.). |
| **W4** Tests | 1 APNs smoke (both fields write under aps); 1 FCM smoke (priority maps correctly). |
| **W5** Preflight + close | Green. ROADMAP row. Merge develop. |

## Closeout

- ~25 lines `types.rs` + ~15 lines `apns.rs` + ~15 lines `fcm.rs` + 2 tests + ROADMAP row.
