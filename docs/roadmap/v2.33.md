# Sentori v2.33 — development plan

Status: **in progress 2026-06-10 — user-based publishing.** Fourteenth version of Push Phase 2. `to: { userFingerprintHex: '<hex>' }` fans out to every device the user has registered in the project.

## NEVER rule

- Zero wire break (new `ToField::User` variant — same union-shape pattern as v2.31 `Topic`).
- Per-project isolation: user X in project A is independent of user X in project B.
- No new schema: `device_tokens.user_fingerprint_hex` exists since v2.7.

## L2 — Work units

| WU | Acceptance |
|---|---|
| **W1** ToField extension | `ToField::User(UserTarget)` with `user_fingerprint_hex: String`. |
| **W2** enqueue_send fanout | Hex-decode fingerprint, query `device_tokens WHERE project_id = $ AND user_fingerprint_hex = $ AND revoked_at IS NULL`, fanout. |
| **W3** Tests | smoke — `as_topic()` returns None for User variant; ensure existing tests still pass. |
| **W4** Preflight + close | green; merge develop. |

## Closeout

~15 lines `types.rs` + ~20 lines `send.rs` + ROADMAP row.
