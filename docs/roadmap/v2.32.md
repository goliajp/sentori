# Sentori v2.32 — development plan

Status: **in progress 2026-06-10 — scheduled sends.** Thirteenth version of Push Phase 2. Adds `sendAt: rfc3339` to the send wire. dispatch_cron already respects `push_sends.next_attempt_at <= now()`; we just set it to `sendAt` at enqueue.

## NEVER rule

- Zero wire break (additive optional field).
- No new dispatcher complexity — existing cron already handles forward-dated `next_attempt_at`.
- `sendAt` in the past is treated as "send now" (clamped via `GREATEST(now(), sendAt)`).

## L2 — Work units

| WU | Acceptance |
|---|---|
| **W1** Wire field | `NativeMessage.send_at: Option<OffsetDateTime>` (camelCase `sendAt`). |
| **W2** enqueue_send | When `send_at` is `Some`, pass it through as `next_attempt_at`; existing INSERT clamps to >= now() at the DB layer. |
| **W3** Tests | unit test on type round-trip (already covered by serde). |
| **W4** Preflight + close | green; merge develop. |

## Closeout

~10 lines `types.rs` + ~10 lines `send.rs` + ROADMAP row.
