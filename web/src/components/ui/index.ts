// Phase 49 sub-A — barrel for the design-system primitives. Anything
// pre-existing in `web/src/components/` that does cross-page styling
// should migrate here so the dashboard has a single import surface
// for layout / status / typography primitives.

export { Chip } from './Chip'
export { InfoBox } from './InfoBox'
export { OverflowItem, OverflowMenu } from './OverflowMenu'
export { Section } from './Section'
